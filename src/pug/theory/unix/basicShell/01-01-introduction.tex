
\subsection{Introduction}
 
 % read-eval-print loop (REPL). strace, kill, killall (are these posix?), [shutdown, reboot], nohup, top, free, (concept of pid), clear, ps[see processes one off then return to terminal], date, uptime. is a ttp. hwclock. timedatectl. /etc/localtime
 